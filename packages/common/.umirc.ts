import { defineConfig } from '@umijs/max';

export default defineConfig({
  hash: true,
  targets: {},
  antd: {},
  dva: {},
  access: {},
  model: {},
  routes: [],
  initialState: {},
  request: {},
  presets: ['umi-presets-pro'],
  npmClient: 'pnpm',
});
